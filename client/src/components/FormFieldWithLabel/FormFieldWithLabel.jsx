import React from "react";
// import * as Styled from "./FormFieldWithLabel.styled";

const FormFieldWithLabel = ({ label, children, ...props }) => {
  // return ( <Styled.Container>
  //     <Styled.Label>
  //         {label}
  //         </Styled.Label>
  //         {{cloneElement(children, { ...props })}}
  //         </Styled.Container> );
  return <div></div>;
};

export default FormFieldWithLabel;
